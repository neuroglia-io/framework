<ellipse 
    cx="0" 
    cy="0"
    rx="@(Bounds.Width.ToInvariantString())"
    ry="@(Bounds.Height.ToInvariantString())"
/>

@code {
    [Parameter] public virtual BoundingBox Bounds { get; set; } = null!;
}
